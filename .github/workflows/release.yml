name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Full test suite must pass before anything ships
        run: python -m unittest discover -s tests -q
      - run: python -m pip install build
      - run: python -m build
      - name: Verify version matches tag
        shell: bash
        run: |
          expected="${GITHUB_REF_NAME#v}"
          actual="$(python -c "import re; m=re.search(r'^version = \"([^\"]+)\"', open('pyproject.toml', encoding='utf-8-sig').read(), re.M); print(m.group(1))")"
          test "$expected" = "$actual"
      - uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-path: "dist/*"
      - name: Publish to PyPI (trusted publishing - no stored token)
        uses: pypa/gh-action-pypi-publish@76e52bc4940c43b57942636913f9b1c568250384 # v1.13.0
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" dist/* --generate-notes --verify-tag
